# 禽康智检 App 版本更新计划

> 制定日期：2026-09-05 · 依据《禽康智检APP开发文档》《prd-v5》《UI_PRO_V2》《qinkang-zhijian-v3》《鸡的疾病知识》《养鸡疑难处置》《禽康智检APP_多角色业务逻辑说明书》· 主色锁定 `#22C55E`

---

## 一、现状基线

| 维度 | 现状 |
|---|---|
| 技术栈 | Expo 52 + React Native · NestJS · PostgreSQL 16 · FastAPI（豆包 `doubao-seed-2-1-turbo` 多模态） |
| **主色** | **`#22C55E`（绿）** — 贯穿启动页、首页头部、主按钮、状态徽标、splash/图标 |
| 部署 | 阿里云轻量服务器（47.109.198.179），三容器（API 3000 / AI 5000 / PostgreSQL 5432），公网端到端诊断已实测通过 |
| 知识库 | 62 章《禽病防治教材》+ 3 本图谱（51 病种）+ 9 附录 + 《养鸡疑难300问》12 章，图谱 490 图经 `/atlas/` 静态服务 |

---

## 二、版本演进回顾（已完成）

| 版本 | 内容 | 状态 |
|---|---|---|
| V1.1 | 知识库 42→62 章 + 图谱 + 养鸡300问第二知识域 + 知识库 tab + 诊断附图注 | ✅ 已部署 |
| V2.0-A | 角色体系（mainRole+subRole）+ 生产管理（批次/每日记录/看板） | ✅ 已部署 |
| V2.0-B | 防控预案（AI 结构化生成）+ 3/7 日回访闭环 | ✅ 已部署 |
| V2.0-C | 环境四类检测（空气/水样/饲料/表面）+ 流调疫情上报/区域统计 | ✅ 已部署 |
| V2.5-A | 商城（商品/订单）+ 诊疗服务单（接单大厅）+ 专家咨询（对话） | ✅ 已部署 |
| V2.5-B | 题库考试 + 实习日志 + 导师批注 + 图谱百科（490 图） | ✅ 已部署 |
| V3.0-A | 角色重构（merchant）+ 登录/注册/选角色页重设计 + 差异化 Tab + 身份认证 + 消息通知 | ✅ 已部署 |
| V3.0-B | 商家端工作台 + 大宗采购 + 兽医佣金 | ✅ 已部署 |
| V3.1 | 顶部状态栏主色统一 + 登录/注册一屏适配 + 体验模式直达 + 底部 Tab 对齐说明书 | ✅ 已部署 |

当前服务器已部署到 V3.1.0（commit `043cb83`），18 张表全建，公网 `/api/docs` 200；版本号统一 3.1.0。

---

## 三、目标态：v3.0《多角色业务逻辑说明书》

一份 8 角色协同平台的完整业务逻辑说明书，核心是：

- **8 大角色**：个体养殖户、合作社/企业、疫控机构、科研院所、兽医服务商、兽药/设备商、教师/导师、学生
- **约 70 个功能模块**（每角色 9~21 个页面）
- **5 条跨角色业务链路**：诊疗服务链、疫情防控链、商品交易链、教学实习链、数据科研链
- **核心飞轮**：多角色使用 → 数据汇聚 → 数据标注 → AI 模型迭代 → 服务更优 → 吸引更多角色

---

## 四、差距分析（目标态 vs 当前）

### 4.1 角色映射缺口

说明书 8 角色，当前代码为 6 主角色 + 10 子角色：

| 说明书 8 角色 | 当前映射 | 差距 |
|---|---|---|
| 个体养殖户 | farmer/small | ✅ 基本覆盖 |
| 合作社/企业 | farmer/cooperative \| enterprise | 🟡 缺企业专属能力（多场/大宗采购/数据导出） |
| 疫控机构 | institution/cdc | 🟡 缺预警/政策/地图/审核 |
| 科研院所 | institution/research | ❌ 工作台几乎全缺 |
| 兽医服务商 | vet | 🟡 缺客户管理/执业认证 |
| **兽药/设备商** | ❌ 无对应（"service" 子角色挂 institution 下，语义含混） | ❌ **全新角色 merchant** |
| 教师/导师 | institution/teacher | 🟡 缺课程/组卷/协同 |
| 学生 | student/learning \| cognitive \| internship | 🟡 缺课程/协同/点评 |

### 4.2 贯穿性缺口（多角色共用，最高复用价值）

**消息通知中心、身份认证、数据导出、大宗采购、客户管理 CRM、审核审批流** —— 6 项在 3~5 个角色里反复出现，是底座能力。

### 4.3 跨角色链路缺口（5 条）

1. **诊疗服务链** 🟡：服务单/咨询已通，缺专家会诊、兽医佣金。
2. **疫情防控链** 🟡：疫情上报已通，缺预警发布/分级/定向推送、政策下发、审核、流调传播链。
3. **商品交易链** 🟡：买家下单已通，缺商家发货履约、大宗采购竞价、物流。
4. **教学实习链** 🟡：实习日志已通，缺课程学习、师生协同、就业推荐。
5. **数据科研链** ❌：数据标注、AI 报告审核、模型迭代整条几乎未建。

---

## 五、版本路线图（V3.0，5 个子阶段 + 阶段 0）

### 阶段 0 —— 提交部署 V2.5-A/B（立刻做）

工作区里已完成但没提交的商城/诊疗/咨询/考试/实习/图谱代码，先 commit + push + 部署 + 打 APK。**这是 V3.0 的地基。**

### V3.0-A —— 角色体系重构 + 平台底座

- 新增「兽药/设备商」角色（`merchant`），对齐 8 角色、修正 "service" 含混定位
- 角色差异化底部 Tab（说明书第五章 8 套 Tab 布局）
- 身份认证（养殖户实名、兽医执业资格、学生学校绑定、商家 GMP 资质）
- 消息通知中心（预警/诊断/政策/订单/系统，全角色贯穿）

### V3.0-B —— 商家端 + 商品交易闭环

- 商家工作台：商品上架/促销/库存、订单处理（发货/退款/售后）、物流跟踪
- 大宗采购（企业询价单 → 供应商竞价 → 比价定标 → 合同）
- 兽医佣金（诊疗方案药品推荐 → 购买返佣）

### V3.0-C —— 疫控机构闭环

- 预警发布/分级/定向推送（风险区内养殖户/兽医/企业）
- 政策下发 + 已读率统计
- 疫情地图热力图（区域下钻）
- 审核管理（疫情上报审核确认）
- 流调传播链分析

### V3.0-D —— 科研院所 + 数据飞轮

- 数据标注工作台（病例图片标注 + 症状标签 + 疾病分类 + 特殊病例复核）
- AI 报告审核（通过/驳回/修正 → 反馈模型）
- 科研协作（协作组 + 文件共享）
- 数据导出（标注数据集、统计报表，多格式）

### V3.0-E —— 教学完善 + 长尾能力

- 课程管理（创建/内容/学习跟踪）、导师工作台、师生协同
- 组卷/发布/监考考试
- AI 诊断练习（学生判断 vs AI 判断 vs 教师点评对比）
- 客户管理 CRM、金融服务、精准广告（长尾）

---

## 六、关键技术决策（沿用既有路线，不推倒重来）

1. **保留 PostgreSQL + 豆包 AI**，不碰 v3 原型的 SQLite。
2. **主色 #22C55E 全链路锁定**，UI_PRO_V2 的 teal（`#0EA5A4`）迁入时换回绿色，建立 design token。
3. **角色模型用 mainRole + subRole 双字段扩展**，新增 `merchant`，不另起炉灶。
4. **消息通知、身份认证、数据导出作为平台底座先行**（V3.0-A 做），避免各角色重复造轮子。
5. **图谱图（490 张）已在 V2.5-B 接入 `/atlas/` 静态服务**，科研数据标注与科普内容直接复用，不重复处理图片。

---

## 七、落地顺序

**阶段 0（提交部署 V2.5-A/B）→ V3.0-A（角色重构+底座）→ B（商家）→ C（疫控）→ D（科研）→ E（教学长尾）**

- 阶段 0 是收尾，必须最先；
- A 把 8 角色和平台底座立起来（尤其"兽药/设备商"这个当前完全缺失的角色）；
- B/C/D 是三大机构端工作台，互相独立、按商业价值排序；
- E 是长尾补全，用户量起来后再上。

---

## 八、V3.1 界面与体验对齐（UI/UX 修正专项）

> 依据用户 2026-09-05 反馈的 5 个界面问题逐项核对属实后的修正计划。版本 **3.0.0 → 3.1.0**，不引入新业务模块，纯界面/交互修正。

### 8.1 确认的问题

| # | 问题 | 现状 | 证据 |
|---|---|---|---|
| 1 | 顶部与主色 #22C55E 不一致 | header 颜色全链路不统一：首页/我的绿底，其余 tab 及全部详情页灰底；登录/注册/选角色浅灰顶；splash/图标仍是 teal `#155E75` | `(tabs)/index.tsx:128`（绿）vs `services.tsx:136`（灰）；`app.json:14/29` `#155E75` |
| 2 | 登录/注册 logo 区占太多、超一屏 | logo 区 `paddingTop:64+paddingBottom:36`、图标 76px、表单间距大，小屏必下滑 | `login.tsx:254`；`register.tsx:215` |
| 3 | 体验模式入口位置不对 | 入口是表单下方整宽卡片，非右上方 | `login.tsx:193` |
| 4 | 体验→选角色未直达主页 | 选角色后仍跳注册；无 token 无法进主页（guest 机制缺失） | `role-select.tsx:137`；`(tabs)/_layout.tsx:20` |
| 5 | 底部 Tab 与说明书不对应 | 当前是「功能页大杂烩」Tab（4~7 个），说明书是「8 角色 × 4 语义 Tab」 | `src/tabs.ts` vs `说明书.md:976-983` |

### 8.2 子阶段

- **阶段 A：顶部主色统一** — 所有页面（tab 页 + 全部详情页 + 登录/注册/选角色）顶部统一 `#22C55E` 绿底 header + 浅色状态栏文字；splash 与 adaptiveIcon 背景 `#155E75` → `#22C55E`。
- **阶段 B：登录/注册一屏适配** — 压缩 logo 区（图标/标题缩小、上下留白收紧）、收紧表单间距、改用不溢出布局，不同设备一屏显示无需下滑。
- **阶段 C：体验模式重构** — 入口上移到登录页右上角；点体验→选角色→**直达对应角色主页**（不走注册）。
- **阶段 D：底部 Tab 对齐说明书** — 重写 `tabs.ts` 为「8 角色 × 4 语义 Tab」，把商城/采购/接单/实习/学习/商品/订单等现有页面挂进 Tab，补齐缺失 Tab。

### 8.3 设计决策（已按推荐默认值，可覆盖）

1. **体验模式 = 前端本地体验**：点体验→选角色后，本地写入临时用户（role/subRole + 体验标识，无后端 token）直接进主页；需登录的接口显示空态并引导登录。零后端改动。
2. **缺失 Tab = 建轻量占位页**：地图/政策/标注/协作/教学/客户等未建 Tab 先建占位页（空态 + 建设中提示 + 入口），Tab 与说明书完全对齐，后续 V3.0-C/D/E 填内容。

### 8.4 Tab 目标映射（说明书对齐）

| 角色 | 目标 4 Tab | 页面落点 |
|---|---|---|
| 个体养殖户 | 首页/诊断/商城/我的 | index / diagnose / mall(入 tab) / profile |
| 合作社/企业 | 首页/数据/采购/管理 | index / production(数据) / bulk-purchase(入 tab) / profile(管理) |
| 疫控机构 | 首页/地图/政策/管理 | index / 地图占位 / 政策占位 / profile |
| 科研院所 | 首页/标注/协作/我的 | index / 标注占位 / 协作占位 / profile |
| 兽医服务商 | 首页/接单/客户/我的 | index / service(接单大厅) / 客户占位 / profile |
| 兽药/设备商 | 首页/商品/订单/管理 | index / mall(商品) / merchant-orders(入 tab) / profile(管理) |
| 教师/导师 | 首页/实习/教学/我的 | index / intern(入 tab) / 教学占位 / profile |
| 学生 | 首页/学习/实习/我的 | index / quiz(学习) / intern(入 tab) / profile |

---

## 九、团队与工作方案（全量开发协作）

### 9.1 团队结构与分工

| 角色 | 职责 |
|---|---|
| 产品/架构（主控） | 锁定需求范围、切分轮次、统筹设计决策与验收 |
| AI 服务工程师 | FastAPI 诊断/对话问诊/预案生成（豆包接入 + 知识库注入） |
| 后端工程师 | NestJS 模块、Prisma schema、JWT、路由注册 |
| 移动端工程师 | Expo/RN 页面、Tab 布局、API 客户端 |
| 质量审查（QA） | 类型检查、db push、端到端实测、公网可达性、代码 review |
| 发布工程 | git 提交/推送、服务器部署、EAS APK 打包 |

### 9.2 工作流程（每轮固定节奏）

1. 需求对齐 → 读说明书/PRD，锁定本轮范围与验收标准
2. 设计 → schema/types 先行，接口契约定清
3. 并行实现 → AI 服务 / API / 移动端各自推进
4. 集成验证 → tsc 全绿 + db push + 端到端脚本实测
5. 发布 → 版本号递增 → commit → push → 服务器部署 → APK 打包
6. 回顾 → 更新说明书/版本计划/长期记忆

### 9.3 质量标准

- **类型安全**：API 与 mobile `tsc --noEmit` 零错误才可提交
- **数据一致性**：Prisma `db push` 成功、新表/枚举落库、公网 `/api/docs` 200
- **功能正确性**：关键链路端到端实测（角色权限、鉴权 403、状态流转、数据落库）
- **主色统一**：所有 UI 严格使用 `#22C55E`，禁止 teal 回潮
- **安全**：JWT 鉴权覆盖新接口，角色越权返回 403，敏感字段不外泄

### 9.4 质量审查与控制

1. 每轮提交前跑 `code-review`（正确性 + 简化）
2. 端到端脚本覆盖正例 + 反例（越权 403 / 缺参 400）
3. 全量完成后，对照《禽康智检APP开发文档.docx》《prd-v5.md》《说明书》逐角色/功能/业务审计
4. 审计产出差距清单 → 再一轮全量更新闭环

---

## 十、V3.2 AI 问诊与诊断升级（本轮）

> 依据用户「统一 5 Tab + 增加 AI 对话问诊 + 升级 AI 兽医诊断」要求，版本 **3.1.0 → 3.2.0**。

### 10.1 范围

- **底部 Tab 统一为 5 个**：8 角色各在其现有 4 Tab 基础上新增「💬问诊」Tab（个体养殖户 = 首页/诊断/问诊/百科/我的）。
- **AI 对话问诊（新）**：FastAPI 新增 `/consult` 多轮对话（角色 persona + PRD 对话问诊提示词 + 知识库注入 + 图片）；NestJS 新增 `consult` 模块（会话持久化 `chat_history`）；移动端新增「问诊」chat 页。
- **AI 兽医诊断升级**：`/diagnose` 携带 role/subRole 自适应 + 混合感染风险评估（`hybridInfectionRisk`）+ 核心依据强化。

### 10.2 技术要点

- 对话历史持久化：Prisma 新增 `ConsultSession`（messages Json，对齐 PRD `chat_history`）
- 角色 persona：farmer/vet/student/teacher/merchant/institution(cdc/research) 各一套系统提示词
- 诊断结果向后兼容：`hybridInfectionRisk` 为可选字段，老数据不受影响

---

## 十、V3.5 全量审计（对照《多角色业务逻辑说明书》逐角色/功能/业务）

> 审计时点：V3.4（3.4.0）。已实现 21 个 API 模块、8 角色 × 5 Tab（13 个真实 Tab 页）。

### 审计结论总览

| 角色 | 说明书要求 | 已实现 | 缺口 |
|---|---|---|---|
| 养殖户 | 21 模块 | 18 | 免疫记录、服务预约(partial)、周边疫情推送(partial) |
| 合作社·企业 | +8 模块 | +3 | 人员管理、设备管理、数据导出、审核审批流、客户管理(下游) |
| 疫控机构 | 9 模块 | 6 | 预警发布、流调记录、数据导出、审核(partial) |
| 科研院所 | 7 模块 | 4 | AI报告审核、科普内容、数据导出 |
| 兽医服务商 | 9 模块 | 7 | 服务定价/排班、专家咨询(partial) |
| 兽药·设备商 | 10 模块 | 6 | 金融服务、精准广告、客户管理(商家)、数据导出 |
| 教师·导师 | 9 模块 | 5 | 课程管理、考试组卷/发布、师生协同、AI报告审核、教学标注 |
| 学生 | 11 模块 | 6 | 课程学习、数据标注(实践)、师生协同、科普文章、AI诊断练习(点评) |

### 未实现功能清单（按优先级）

**P0（核心业务闭环，优先补齐）**
1. **免疫记录**（养殖户）：免疫计划 + 接种记录 + 到期提醒 —— 养殖户日常核心
2. **预警发布**（疫控机构）：预警分级（一般/较大/重大）+ 定向推送风险区 —— V3.0-C 核心闭环
3. **流调记录**（疫控机构）：流行病学调查 + 传播链

**P1（高频刚需）**
4. **课程管理 + 课程学习**（教师/学生）：课程创建 + 视频/课件 + 学习进度 —— 教学核心
5. **考试组卷**（教师）：题库组卷 + 发布 + 成绩分析
6. **数据导出**（企业/疫控/商家）：Excel/CSV/PDF
7. **客户管理扩展**（企业下游客户 / 商家客户）：现 Customer 仅兽医可用

**P2（增强）**
8. **AI报告审核**（科研/教师）：AI诊断报告人工审核 + 质量控制
9. **科普内容/文章**（科研/学生）：文章管理 + 拓展阅读
10. **人员管理 + 设备管理**（企业）
11. **金融服务**（商家：账期/贷款/保险）
12. **精准广告**（商家）
13. **师生协同**（教师/学生）

### V3.6 更新计划（全量补齐 P0+P1）

- **V3.6-A 免疫记录**：Immunization 模型（plan+record）+ 到期提醒 + 移动端免疫 tab 页（挂养殖户生产/百科入口）
- **V3.6-B 预警发布 + 流调**：EpidemicAlert 模型（分级+推送范围+通知）+ 流调记录模型；疫控机构发布预警→定向推送→通知
- **V3.6-C 课程 + 组卷**：Course 模型（章节+学习进度）+ 教师组卷/发布考试 + 学生课程学习
- **V3.6-D 数据导出 + 客户扩展**：导出接口（CSV）+ Customer owner 角色扩到 enterprise/merchant

### V3.6-A 免疫记录（已完成 2026-09-05）

- **Schema**：新增 `Immunization` 模型 + `ImmunizationMethod`（injection/water/drop_eye/drop_nose/spray/other）+ `ImmunizationStatus`（planned/completed/overdue）枚举；User/PoultryHouse/Batch 加反向关系
- **API**：新增 immunization 模块 7 路由（创建/列表?status=/stats/reminders/详情/更新/删除），仅养殖户可访问（vet 等 403）
- **到期提醒**：`GET /immunization/reminders` 返回「已逾期 + 未来 7 天到期」；`stats` 返回 total/planned/completed/overdue/dueSoon
- **移动端**：新增 `app/immunization.tsx`（列表 + 免疫提醒卡 + 内联表单 + 标记完成/逾期 + 删除）+ `src/api/immunization.ts`；首页为养殖户新增「💉 免疫记录」快捷入口
- **版本号**：3.4.0 → 3.5.0
- **验证**：端到端 14/14 通过（含 vet 越权 403×2、缺参 400、删除后 404）

### V3.6-B 预警发布 + 流调记录（已完成 2026-09-05）

- **Schema**：新增 `EpidemicAlert`（分级+定向推送+状态）+ `Epidemiology`（传染源/传播链/处置措施/结案）模型 + `EpidemicAlertLevel`（general/major/severe）+ `EpidemicAlertStatus`（active/resolved）+ `EpidemiologyStatus`（investigating/processing/completed）枚举
- **API**：epidemic-alert 模块 6 路由（发布[定向推送通知]/stats/list/详情/更新[解除]/删除）+ epidemiology 模块 6 路由（创建/stats/list?status=/详情/更新/删除），均仅疫控机构可写（其他角色 403）
- **预警推送**：发布预警时按 audience 角色（空=全体）自动 fanout 生成 `warning` 类型通知
- **移动端**：新增 `app/epidemic-alert.tsx` + `app/epidemiology.tsx` 两个页面（列表+统计+内联表单+状态流转+删除）；疫控「地图」Tab 加「📢预警发布」「📋流调记录」两个快捷入口
- **验证**：端到端 17/17 通过（含定向推送通知送达、越权 403×3、状态流转、删除）

### V3.6-C 课程 + 组卷（已完成 2026-09-06）

- **Schema**：新增 `Course`（章节 Json + 状态）+ `CourseProgress`（学习进度 0-100 + 已完成章节）+ `ExamPaper`（questionIds Json + 总题数/总分/时长/状态）模型 + `CourseStatus`（draft/published）+ `ExamPaperStatus`（draft/published/archived）枚举
- **API**：course 模块 7 路由（创建/列表/详情[含我的进度]/更新/删除/更新进度/我的进度）+ exam-paper 模块 6 路由（创建指定题/智能组卷 compose/列表/详情[含完整题目]/更新/删除）；教师=admin 或 institution+subRole=teacher，学生=student 角色
- **智能组卷**：`POST /exam-paper/compose` 按章节/难度/数量从题库（30 道种子题）自动抽题生成试卷
- **移动端**：app/course.tsx（教师建课+章节，学生学习+进度）+ app/exam-paper.tsx（教师智能组卷，学生看已发布试卷）；学生「学习」Tab 加「课程学习」「在线考试」入口，教师「教学」Tab 加「课程管理」「考试组卷」快捷入口
- **验证**：端到端 14/14 通过（含学生建课 403、农户组卷 403、进度更新、组卷详情含题目）
- **版本号**：3.6.0 → 3.7.0

### V3.6-D 数据导出 + 客户扩展（已完成 2026-09-06，V3.6 全量收官）

- **数据导出**：新增 export 模块，`GET /export/:entity` 返回带 UTF-8 BOM 的 CSV（Excel 中文友好），支持 diagnoses/immunizations/epidemic-records/orders/daily-records 5 类；按角色过滤（诊断/免疫/生产=本人，疫情=疫控可全量，订单=买家或商家按 merchantId）
- **客户管理扩展**：Customer 访问控制从「仅兽医/技术员」扩到「兽医/技术员/企业/合作社/商家」（farmer+subRole=enterprise/cooperative + merchant 均可管理客户）
- **移动端**：「管理」Tab 为企业/合作社/商家新增「客户管理」入口（指向已有客户页）
- **版本号**：3.7.0 → 3.8.0
- **验证**：端到端 10/10 通过（5 类 CSV 导出、非法实体 400、企业/商家建客户、个体养殖户 403）

### V3.6 全量完成总结

V3.6 计划（A 免疫记录 → B 预警发布+流调 → C 课程+组卷 → D 数据导出+客户扩展）**全部完成**：新增 8 张表（immunizations/epidemic_alerts/epidemiologies/courses/course_progresses/exam_papers + 复用的 notifications），新增 4 个 API 模块（immunization/epidemic-alert/epidemiology/course/exam-paper/export 共 6 个），移动端新增 6 个页面。版本 3.4.0 → 3.8.0。

## 十一、V3.9 AI 对话问诊全量升级（对齐《AI对话问诊开发文档_豆包2.1turbo》）

### 11.1 背景与目标

用户提供《禽康智检APP_AI对话问诊开发文档_豆包2.1turbo》（v1.0），要求将现有「问诊」Tab（V3.2 已建的多轮对话问诊）全量改进升级，改名「AI问诊」，并修复「network request failed」（对话问诊用不起）问题。

### 11.2 问题定位

端到端实测（公网注册→登录→POST /consult/message）返回 201，约 11.6s 正常回复——**后端链路（移动端→API→AI→豆包）完全正常**。`network request failed` 是移动端网络层报错：豆包多模态 + 62 章知识库注入导致单轮推理约 11.6s，且移动端 fetch 无超时、无友好错误提示，用户感知为「用不起」。修复与升级并进。

### 11.3 本次改动

**AI 服务（apps/ai/src/server.py）**
- `/consult` 提示词对齐 doc 第五章：问诊流程（倾听→追问→诊断→防控）+ 重要规则（禽病限定/处方权/危重预警/不编造/休药期）+ 输出格式（分段/编号/加粗/疾病名【】标注）
- 回复末尾强制追加医疗免责声明（模型未写则代码兜底）
- 输出新增 `related_diseases`（关联疾病标签，doc 3.1）
- `_build_reference` 加 `top_k` 参数，问诊从 3 章收紧到 2 章降延迟

**API（apps/api/src/modules/consult）**
- 透传 `relatedDiseases` 到消息与响应
- `callAI` 的 fetch 加 AbortController 90s 超时，超时返回友好提示

**移动端（apps/mobile）**
- Tab/页面标题「问诊」→「AI问诊」（tabs.ts + _layout.tsx + consult.tsx）
- 新增快捷问题标签（doc 3.1）：鸡拉稀/呼吸啰音/产蛋下降/突然死亡等，一键提问
- 新增问诊历史会话列表（doc 3.2）：查看/继续历史会话
- 新增关联疾病标签展示（doc 3.1）
- 登录引导「去登录」改为真实跳转
- `client.ts` 全站 fetch 加 60s 超时 + 网络错误/超时友好提示（修复 network request failed 的裸报错）

**版本号**：3.8.0 → 3.9.0（5 个 package.json + app.json version/buildNumber 13/versionCode 13 + login/profile 回退值）

### 11.4 遗留（后续轮次）

- 流式输出（SSE）：doc 强调逐字输出提升体验，需 RN 侧 SSE 支持（当前非流式 + typing 指示器）
- 诊断报告页（doc 3.3）：结构化报告生成/分享/保存
- 学生模式（doc 3.4）：问诊思路展示 + 笔记 + 测验题
- 语音输入、内容安全过滤、限流、Token 计数、缓存降级（doc 第八章）


---

## 十二、V4.0「AI诊断」全量重构（对齐《AI兽医诊断功能开发文档_v2.md》）

### 12.1 定位

把底部 Tab「诊断」升级为「**AI诊断**」，从「纯图片→豆包多模态诊断」重构为文档 v2.0 定义的：

**专业结构化信息采集（9大维度） + 多模态提交豆包2.1 turbo深度诊断 + 规则引擎离线兜底**

与「AI问诊」（对话问诊，V3.9）形成"快慢双引擎"：
- **AI诊断**：结构化 9 维表单，专业鉴别诊断，10-20s，可离线兜底，标准化病例数据入库
- **AI问诊**：自然语言多轮对话，快速咨询，3-10s

### 12.2 9 大信息采集维度

1. 基本信息（禽种/品种/日龄/存栏/发病/死亡/饲养方式/养殖阶段）
2. 主诉与病史（主诉/发病时间/病程/经过/死亡率趋势/传播速度/既往史）
3. 临床症状（8类结构化标签勾选：一般状态/消化道/呼吸道/生殖道/神经/皮肤黏膜/运动/其他 + 照片）
4. 剖检病变（8类结构化标签勾选：皮下肌肉/消化/呼吸/循环/泌尿生殖/免疫器官/神经/其他 + 照片）
5. 实验室检测（血清学/病原学/细菌学/寄生虫/生化/血常规）
6. 免疫史（程序/最近免疫/反应/抗体/失败史）
7. 用药史（最近用药/效果/过敏/保健/休药期）
8. 环境与管理（温湿度/通风/密度/饲料/饮水/光照/天气/人员/周边疫情/生物安全）
9. 流行病学（引种/疫苗来源/鸡群来源/混养/野鸟/同类场/病死处理）

### 12.3 实现要点

- **AI 服务** 新增 `/vet-diagnose`：9 维结构化 prompt 组装（对齐 doc 4.1）+ 知识库注入 + 多模态图片 + 结构化报告输出（首要/次要/已排除诊断 + 诊断依据 + 鉴别检测 + 防控方案[紧急/用药/免疫/消毒/管理] + 随访建议 + 免责声明 + 危重预警）
- **API** 新增 `vet-diagnosis` 模块：病例 CRUD + 触发 AI 诊断（异步）+ 离线结果保存 + 症状/病变标签目录 + 反馈
- **Prisma** 新增 `VetCase` 模型（9 维 Json + 诊断结果 + 引擎标识 + 状态机 draft/submitted/analyzing/completed/failed/offline）
- **移动端**
  - 移植 v3 原型规则引擎（26 病：症状/病变权重 + 特征性症状加分 + 鉴别诊断 + 治疗方案 + 防控 + 风险等级），作为**离线兜底**
  - 结构化症状/病变标签目录（与规则引擎症状名对齐，保证离线匹配命中）
  - 重写 `diagnose.tsx` 为 9 维分步表单（进度条 + 标签勾选 + 图片上传 + 草稿）
  - 新增结构化诊断报告页（结论/依据/鉴别/防控/随访/免责）+ 病例库页（统计+检索+列表）
  - Tab/标题「诊断」→「AI诊断」

### 12.4 版本号

3.9.0 → **4.0.0**（buildNumber 14 / versionCode 14）

### 12.5 后续遗留

- SSE 流式输出（首字 <3s）、草稿自动保存、诊断结果结构化解析入库复用
- 图片压缩到 1024px、病例导出（Excel/PDF）、规则库云端同步（/rules/sync）
- 与商城/在线问诊/实验室检测/回访/疫情预警的联动入口

## 13. 养鸡知识库全量改造（V4.4.0）

### 13.1 需求
- 顶部副标题改为「收录鸡病 X 种 · 鸡病图片 X 张 · 养鸡实用技巧 X 篇」（三数量直接统计）
- 图谱百科与疾病防治合并：查看某病时展示图文并茂的疾病（正文 + 图谱图片）
- 养鸡问答 → 养鸡技巧：将《养鸡疑难300问》299 问按 12 类分门别类，作为咨询文章展示，带分类选择器

### 13.2 统计口径（直接统计）
- 鸡病数量 = 59（62 章 - 3 章总论 ch01~03）
- 鸡病图片 = 490（apps/api/public/atlas/*.jpg 实际文件数，与 manifest 一致）
- 养鸡实用技巧 = 298（12 章 `### Q` 问答条目；ch01 原文抽取缺失 Q2/Q11 两问）
- 12 分类：品种与繁育/孵化/营养饲料/雏鸡/育成/产蛋/肉仔鸡/肉种鸡/无公害/鸡舍设备/经营管理/疾病防治

### 13.3 实现
- **AI 服务** `knowledge.py`：`disease_count`/`figure_count` 统计属性；`FarmingKnowledge` 按 `### QN` 拆分问答文章（遇 `## ` 小节终止，避免混入「关键处置要点」）；`lookup_figures` 返回 `images`（图注 caption + figX-YYY.jpg 映射）
- **AI 服务** `server.py`：新增 `GET /knowledge/stats`、`GET /knowledge/farming/index`、`GET /knowledge/farming/article/{id}`
- **API**：knowledge 模块新增 stats/farming-index/farming-article 三个端点；`chapter` 把图号映射为 `/atlas/xxx.jpg`
- **types**：新增 `KnowledgeStats`/`FarmingCategory`/`FarmingArticle`/`FarmingIndex`
- **移动端**：`knowledge.tsx` 统计副标题 + 养鸡技巧分类选择器（横向 chips）+ 咨询文章列表；疾病详情页 `knowledge/[id].tsx` 图文并茂（图谱图片 + 图注）；新增 `farming-article/[id].tsx` 问答详情页

### 13.4 版本号
4.3.0 → **4.4.0**（buildNumber 18 / versionCode 18）
